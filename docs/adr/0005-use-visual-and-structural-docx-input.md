# Use visual and structural DOCX input for Block Recognition

DOCX bootstrap provides the model with synchronized rendered pages and structured document extraction. Visual input preserves layout cues needed to infer Components such as tabs, quotes, and carousels, while structural input preserves exact text, image identity, and reading order that screenshots alone cannot reliably recover.
